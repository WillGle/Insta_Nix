{
  "layer": "top",
  "position": "top",
  "height": 30,
  "modules-left": [
    "pulseaudio",
    "custom/power_profile",
    "custom/refresh_rate",
    "hyprland/workspaces"
  ],
  "modules-center": [
    "clock"
  ],
  "modules-right": [
    "custom/gpu_info",
    "custom/cpu_info",
    "custom/watt_info",
    "custom/memory_info",
    "tray"
  ],
  "hyprland/workspaces": {
    "format": "{name}"
  },
  "clock": {
    "format": " {:%a %d %b  %H:%M}",
    "tooltip-format": "<big>{:%A, %d %B %Y}</big>\n<tt>{calendar}</tt>",
    "on-click": "gsimplecal"
  },
  // ─── Memory (custom with top-process tooltip) ─────────────────────
  "custom/memory_info": {
    "exec": "~/.local/bin/waybar-memory-info",
    "return-type": "json",
    "interval": 1,
    "tooltip": true
  },
  "custom/cpu_info": {
    "exec": "~/.local/bin/waybar-power-monitor cpu",
    "return-type": "json",
    "interval": 1,
    "tooltip": true
  },
  // ─── Audio ────────────────────────────────────────────────────────
  "pulseaudio": {
    "format": "{icon}  {volume}%",
    "format-bluetooth": " {volume}%",
    "format-muted": "󰖁",
    "format-bluetooth-muted": " 󰖁",
    "on-click": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
    "scroll-step": 1,
    "tooltip-format": "{icon} {volume}%",
    "format-icons": {
      "default": [
        ""
      ]
    }
  },
  // ─── Hardware Monitors (consolidated temp + power) ────────────────
  "custom/gpu_info": {
    "exec": "~/.local/bin/waybar-power-monitor gpu",
    "return-type": "json",
    "interval": 1,
    "tooltip": true
  },
  "custom/watt_info": {
    "exec": "~/.local/bin/waybar-power-monitor pwr",
    "return-type": "json",
    "interval": 2,
    "tooltip": true
  },
  "custom/power_profile": {
    "exec": "~/.local/bin/waybar-power-monitor prof",
    "return-type": "json",
    "interval": 30,
    "signal": 2,
    "on-click": "~/.local/bin/waybar-power-monitor toggle",
    "tooltip": true
  },
  // ─── Refresh Rate ─────────────────────────────────────────────────
  "custom/refresh_rate": {
    "exec": "~/.local/bin/waybar-refresh-label",
    "return-type": "text",
    "interval": 1,
    "on-click": "~/.local/bin/waybar-refresh-toggle",
    "tooltip": false
  }
}